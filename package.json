{
  "name": "alert-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -n backend,admin -c blue,green \"npm run dev -w backend\" \"npm run dev -w admin\"",
    "build": "npm run build -w backend && npm run build -w admin",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w admin"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "eslint": "^9.29.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^16.2.0",
    "prettier": "^3.5.0",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.34.0"
  }
}
